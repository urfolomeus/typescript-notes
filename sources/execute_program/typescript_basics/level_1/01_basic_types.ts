// We can specify the type of a variable with a type annotation.
const sumNumber: number = 1 + 2;
console.log(sumNumber);

// If we assign a value of the wrong type to a variable, we'll get a type error.
const sumString: number = 'any' + 'thing';
console.log(sumString);
